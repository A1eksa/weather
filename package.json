{
  "name": "weather",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "browser-router": "^0.2.0",
    "node-fetch": "^3.2.6",
    "react-router-dom": "^6.3.0"
  }
}
